Drop client logo PNGs here. Filenames must match the `slug` in lib/data.ts (CLIENTS):

  pbc.png        -> PBC
  emaar.png      -> Emaar Land
  pizza.png      -> Pizza by Hira
  fitness.png    -> Fitness Total
  4d.png         -> 4D Für Dich
  super.png      -> Super Marketer
  mtgar.png      -> MTGAR

If a file is missing, the marquee falls back to the client's name as text.
Transparent PNGs on dark backgrounds look best.
